<div style="font-family:Arial,sans-serif;line-height:1.5;color:#0f172a;">
  <h2 style="margin:0 0 10px;">Reinitialisation du mot de passe</h2>
  <p>Bonjour {{ user.nom ?? 'utilisateur' }},</p>
  <p>Cliquez sur ce lien pour definir un nouveau mot de passe :</p>
  <p><a href="{{ reset_url }}">{{ reset_url }}</a></p>
  <p>Ce lien expire le {{ expires_at|date('d/m/Y H:i') }}.</p>
  <p>Si vous n'avez pas demande ce changement, ignorez cet email.</p>
</div>
